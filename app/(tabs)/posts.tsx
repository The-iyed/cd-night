import { View } from '@/components/Themed';
import {Button} from 'react-native';

export default () => {
    return <View>
        <Button title={"Create Your Post"}/>
        Hello this is the section for posts
    </View>
}